(**)
(* A simple comment*)
(* A 
multiline 
 comment *)

let x : int = 42 in (* A comment after code *)
let y : int = 665971 in
(* A multiline comment
  in between code.
*)
x + (* in the middle of code *) y
(* comment before line of code *) end
end
