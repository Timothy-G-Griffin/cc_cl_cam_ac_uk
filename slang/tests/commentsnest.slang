let x : int = 3 in (* A comment with a (* nested comment*) inside it. *)
let y : int = 7 in
(* A multiline comment
  (*with a nested comment*)
 inside it.
*)
x + y
(*comment before code*) end
end

(* A comment with a (*nested comment with a (*nested comment*)*) is cool.*)

(*(*(*(*(*(*(*(*(*(* very nested comment *)*)*)*)*)*)*)*)*)*)